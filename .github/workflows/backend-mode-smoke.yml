name: backend-local-external-smoke

on:
  push:
    branches:
      - main
      - 'codex/**'
  pull_request:

jobs:
  backend-local-external-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'
      - name: Install package + test deps
        run: |
          python -m pip install --upgrade pip
          pip install -e . pytest
      - name: Run backend/local-external integration smoke tests
        run: |
          pytest tests/test_gateflow_cli_backend_sync.py -q
          pytest tests/test_gateflow_cli_connect_local_external.py -q
